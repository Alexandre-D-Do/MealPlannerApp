<ResourceDictionary x:Class="MealPlannerApp.Styles.ApplicationWindowStyles"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <SolidColorBrush x:Key="Window.Static.Background" Color="#1E1E1E"/>
    <Style x:Key="ApplicationWindow" TargetType="{x:Type Window}" >
        <Setter Property="Background" Value="{StaticResource Window.Static.Background}" />
        <Setter Property="WindowStyle" Value="None"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0.65,0.65,0.65,0.65" />
        <Setter Property="WindowChrome.WindowChrome">
            <Setter.Value>
                <WindowChrome GlassFrameThickness="0" CornerRadius="0" CaptionHeight="0"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContentControl}" >
                    <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true" >
                        <ContentPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <EventSetter Event="MouseLeftButtonDown" Handler="Window_MouseLeftButtonDown"/>
    </Style>
    <Style TargetType="{x:Type ContentControl}">
        <Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>
    </Style>
    <ControlTemplate x:Key="ErrorTemplate">
        <StackPanel HorizontalAlignment="Left">
            <Border BorderBrush="Red" BorderThickness="1" HorizontalAlignment="Left" SnapsToDevicePixels="True" Padding="0">
                <AdornedElementPlaceholder/>
            </Border>
            <TextBlock Foreground="Red" FontSize="12" Text="{Binding [0].ErrorContent}" Margin="0,2,0,0" />
        </StackPanel>
    </ControlTemplate>
    <Style x:Key="TextBoxErrorTemplate" TargetType="TextBox">
        <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ErrorTemplate}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>
    <Style TargetType="DatePicker">
        <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ErrorTemplate}"/>
    </Style>
</ResourceDictionary>